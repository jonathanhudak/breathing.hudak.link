export const ONE_SECOND = 1000;
export const ONE_MINUTE = ONE_SECOND * 60;
export const ONE_HOUR = ONE_MINUTE * 60;

// @TODO Should be 5.5s but rounding up since timer state is only in seconds rn
export const COHERENCE_BREATH_DURATION = 6000;
